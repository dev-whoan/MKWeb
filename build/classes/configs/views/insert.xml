<?xml version="1.0" encoding="UTF-8"?>

<views>
	<Control name="insert" debug="error" dir="/views/root" dir_key="/old" page="insert.jsp">
		
		<Service type="sql">
			<Sql id="insertUser" method="post" />
			<Parameter name="param" />
			<Value>
				@set(param.user_name = '%')
				@set(param.user_info = '%')
			</Value>
		</Service>
				
		<!-- 
		<Service type="browser">
			<Parameter name="param" />
			<Value>
				@set(${browser.LAN="ko"})
				@set(${browser.agent="chrome"})
				@set(${browser.mob="N")
			</Value>
		</Service>
		 -->
		
	</Control>
</views>